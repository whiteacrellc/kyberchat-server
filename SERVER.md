
### Server migration to k8s GKE mysql and redis.

The goal is to create a set of docker images ant are run by docker compose.

1. read all the files in the cloudrun directory and create a set of k8s nodes that will providfe those APIs.

2. Keep mydwl as the relational database and run the mysql instance  in Cloud SQL 